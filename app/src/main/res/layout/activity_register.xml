<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="16dp">

    <ImageView
        android:id="@+id/logoImageRegister"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:src="@drawable/logo" />

    <EditText
        android:id="@+id/usernameInput"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Tên Đăng Nhập"
        android:inputType="text"
        android:autofillHints="username"
        android:padding="16dp"
        android:minHeight="48dp"/>

    <EditText
        android:id="@+id/passwordRegisterInput"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Nhập Mật Khẩu"
        android:inputType="textPassword"
        android:autofillHints="password"
        android:padding="16dp"
        android:minHeight="48dp"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Vui lòng đăng ký để xác nhận và tránh các sự cố về tài khoản!"
        android:textColor="@color/colorError" />

    <Button
        android:id="@+id/registerButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="ĐĂNG KÝ TÀI KHOẢN"
        android:backgroundTint="@color/colorPrimary" />

    <Button
        android:id="@+id/backButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="QUAY LẠI"
        android:onClick="navigateToLogin"/>

    <TextView
        android:id="@+id/forgotPasswordTextRegister"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Mất Mật Khẩu?" />
</LinearLayout>